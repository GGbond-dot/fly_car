# 飞行小车项目快速参考手册

## 项目结构速查

```
fly_car/
├── src/
│   ├── pid_control_pkg/         # PID位置控制
│   ├── uart_to_stm32/           # STM32串口通信
│   ├── car_driver/              # 电机驱动（Python）
│   ├── bluetooth/               # 蓝牙通信
│   ├── bluesea2/                # 激光雷达
│   ├── activity_control_pkg/    # 路径规划与控制
│   ├── my_carto_pkg/            # Cartographer地图构建
│   ├── openmv_bridge/           # OpenMV视觉
│   ├── serial_comm/             # 串口通信基础库
│   ├── pid_controller/          # PID控制器基础
│   └── my_launch/               # Launch启动文件
```

---

## 常用命令速查表

### ROS2命令

```bash
# 编译
colcon build                                    # 编译所有包
colcon build --packages-select <pkg_name>      # 编译指定包
colcon build --symlink-install                 # 符号链接安装（Python开发推荐）

# 运行
source install/setup.bash                       # 加载环境
ros2 run <pkg> <executable>                     # 运行节点
ros2 launch <pkg> <launch_file>                 # 运行launch文件

# 查看
ros2 topic list                                 # 列出所有话题
ros2 topic echo <topic_name>                    # 查看话题内容
ros2 topic info <topic_name>                    # 查看话题信息
ros2 topic hz <topic_name>                      # 查看话题频率

ros2 node list                                  # 列出所有节点
ros2 node info <node_name>                      # 查看节点信息

ros2 param list                                 # 列出所有参数
ros2 param get <node_name> <param_name>         # 获取参数值
ros2 param set <node_name> <param_name> <value> # 设置参数值

# TF调试
ros2 run tf2_tools view_frames                  # 生成TF树图
ros2 run tf2_ros tf2_echo <source> <target>     # 查看坐标变换

# 录制与回放
ros2 bag record -a                              # 录制所有话题
ros2 bag record <topic1> <topic2>               # 录制指定话题
ros2 bag play <bag_file>                        # 回放录制的数据
```

---

## 关键话题速查

| 话题名称 | 消息类型 | 方向 | 说明 |
|---------|---------|------|------|
| `/target_position` | Float32MultiArray | 订阅 | 目标位置 [x_cm, y_cm, z_cm, yaw_deg] |
| `/target_velocity` | Float32MultiArray | 发布/订阅 | 目标速度 [vx, vy, vz, vyaw] |
| `/velocity_map` | Twist | 订阅 | 全局坐标系速度指令 |
| `/height` | Int16 | 发布 | 当前高度（厘米） |
| `/is_st_ready` | UInt8 | 发布 | STM32就绪状态 |
| `/mission_step` | UInt8 | 发布 | 任务步骤 |
| `/bluetooth_data` | UInt8MultiArray | 订阅 | 蓝牙接收的数据 |
| `/wheel_speeds` | Float32MultiArray | 发布 | 四轮速度 |

---

## PID参数快速调节指南

### 调节步骤

1. **设置所有参数为0，只调Kp**
   - 从小值开始（0.1, 0.2, 0.5...）
   - 逐渐增大直到出现轻微震荡
   - 减小到震荡消失的80%左右

2. **调节Kd**
   - 从小值开始（Kp的1/10左右）
   - 逐渐增大以减少超调
   - 过大会使系统反应迟钝

3. **调节Ki**（如果有稳态误差）
   - 从很小值开始（Kp的1/100）
   - 缓慢增加以消除稳态误差
   - 过大会引起震荡和超调

### 现象与对策

| 现象 | 可能原因 | 解决方案 |
|-----|---------|---------|
| 震荡不止 | Kp过大 | 减小Kp |
| 反应慢 | Kp太小 | 增大Kp |
| 超调严重 | Kd太小或Ki太大 | 增大Kd或减小Ki |
| 稳态误差 | Ki太小或死区太大 | 增大Ki或减小死区 |
| 噪声敏感 | Kd过大 | 减小Kd或增加滤波 |

### 本项目参数参考

```cpp
// XY方向位置控制
pid_x/y: kp=0.5, ki=0.0, kd=0.1
max_output=36.0 cm/s, deadzone=0.6 cm

// Yaw角度控制
pid_yaw: kp=1.0, ki=0.0, kd=0.2
max_output=30.0 deg/s, deadzone=0.5 deg

// Z方向高度控制
pid_z: kp=0.9, ki=0.0, kd=0.15
max_output=35.0 cm/s, deadzone=0.6 cm
```

---

## 坐标系变换速查

### 常用坐标系

```
map (全局地图)
 └── odom (里程计)
      └── base_link (机器人本体)
           └── laser_link (激光雷达)
```

### 变换公式

**旋转角度θ（逆时针为正）**

**全局 → 本体:**
```
x_local = cos(θ) * x_global + sin(θ) * y_global
y_local = -sin(θ) * x_global + cos(θ) * y_global
```

**本体 → 全局:**
```
x_global = cos(θ) * x_local - sin(θ) * y_local
y_global = sin(θ) * x_local + cos(θ) * y_local
```

**角度归一化:**
```python
def normalize_angle(angle):
    while angle > π:
        angle -= 2π
    while angle < -π:
        angle += 2π
    return angle
```

**单位转换:**
```cpp
// 弧度 ↔ 角度
radians = degrees * π / 180
degrees = radians * 180 / π

// 米 ↔ 厘米
meters = centimeters / 100.0
centimeters = meters * 100.0
```

---

## 麦克纳姆轮速度计算

### 标准配置（X型）

```
    前方
  1 ─── 2
  │     │
  4 ─── 3
```

**已知**: 本体速度 (vx, vy, ω)
- vx: 前进速度（前为正）
- vy: 横移速度（左为正）
- ω: 角速度（逆时针为正）

**求解**: 四轮速度 (v1, v2, v3, v4)

```cpp
double lx = wheelbase / 2.0;   // 前后轮距一半
double ly = track_width / 2.0; // 左右轮距一半
double L = lx + ly;            // 轮子到中心的距离系数

v1 = vx - vy - ω * L;  // 前左
v2 = vx + vy - ω * L;  // 前右
v3 = vx + vy + ω * L;  // 后右
v4 = vx - vy + ω * L;  // 后左
```

### 特殊运动

| 运动 | vx | vy | ω | 轮速比例 (1:2:3:4) |
|-----|----|----|---|-------------------|
| 前进 | + | 0 | 0 | 1:1:1:1 |
| 后退 | - | 0 | 0 | -1:-1:-1:-1 |
| 左移 | 0 | + | 0 | -1:1:1:-1 |
| 右移 | 0 | - | 0 | 1:-1:-1:1 |
| 逆时针旋转 | 0 | 0 | + | -1:-1:1:1 |
| 顺时针旋转 | 0 | 0 | - | 1:1:-1:-1 |

---

## 串口命令速查

### 电机配置命令

```python
# 电机类型 (1-5)
"$mtype:2#"

# 死区 (PWM值)
"$deadzone:1300#"

# 磁环线数
"$mline:13#"

# 减速比
"$mphase:20#"

# 轮子直径 (mm)
"$wdiameter:80.00#"
```

### 电机控制命令

```python
# 速度控制 (mm/s)
"$spd:100,100,100,100#"

# PWM控制 (-255 到 255)
"$pwm:128,128,128,128#"

# 数据上传模式
"$upload:0,0,1#"  # 上传速度数据
```

### 数据格式

```python
# 接收编码器总数
"$MAll:1000,1000,1000,1000#"

# 接收实时编码器
"$MTEP:100,100,100,100#"

# 接收电机速度
"$MSPD:50.5,50.5,50.5,50.5#"
```

---

## 调试技巧

### 1. 查看实时数据

```bash
# 查看话题数据
ros2 topic echo /target_position

# 以固定频率查看
ros2 topic hz /target_velocity

# 查看话题统计
ros2 topic bw /velocity_map
```

### 2. 发布测试消息

```bash
# 发布简单消息
ros2 topic pub /topic_name std_msgs/msg/String "data: 'test'"

# 发布数组消息
ros2 topic pub /target_position std_msgs/msg/Float32MultiArray \
  "{data: [100.0, 50.0, 30.0, 45.0]}"
```

### 3. 日志级别设置

```bash
# 设置日志级别为DEBUG
ros2 run package_name node_name --ros-args --log-level DEBUG

# 设置特定节点的日志级别
ros2 run package_name node_name --ros-args \
  --log-level node_name:=DEBUG
```

### 4. RQT图形化工具

```bash
# 节点图
rqt_graph

# 话题监视器
rqt_topic

# 参数调节
rqt_reconfigure

# 绘图
rqt_plot /topic_name/data[0]
```

---

## 常见错误代码

### 编译错误

```bash
# 缺少依赖
rosdep install --from-paths src --ignore-src -r -y

# 清理重新编译
rm -rf build install log
colcon build

# 检查包依赖
rosdep check --from-paths src --ignore-src
```

### 运行时错误

```bash
# 权限问题
sudo chmod 666 /dev/ttyUSB0
sudo usermod -a -G dialout $USER

# 串口被占用
sudo lsof | grep ttyUSB0
kill -9 <PID>

# 环境变量未设置
source /opt/ros/humble/setup.bash
source install/setup.bash
```

---

## 性能优化建议

### 1. 控制频率选择

- **PID控制**: 50-100 Hz（本项目：50 Hz）
- **传感器读取**: 根据传感器规格
- **通信**: 不低于控制频率

### 2. 计算优化

- 避免在控制循环中进行复杂计算
- 使用查表法替代三角函数
- 预计算常量值

### 3. 通信优化

- 使用合适的QoS设置
- 避免不必要的话题订阅
- 批量发送数据而非逐个发送

---

## 安全注意事项

### 1. 速度限制

```cpp
// 设置合理的速度上限
max_linear_velocity: 36.0 cm/s
max_angular_velocity: 30.0 deg/s
max_vertical_velocity: 40.0 cm/s
```

### 2. 故障保护

- 通信超时检测
- 传感器数据有效性检查
- 急停机制

### 3. 测试建议

- 先在仿真环境测试
- 从低速开始逐步增加
- 准备随时手动停止的方法

---

## 重要公式汇总

### PID控制
```
u(t) = Kp·e(t) + Ki·∫e(t)dt + Kd·de(t)/dt
```

### 坐标旋转（全局→本体）
```
[x_local]   [ cos(θ)  sin(θ)] [x_global]
[y_local] = [-sin(θ)  cos(θ)] [y_global]
```

### 麦克纳姆轮
```
v1 = vx - vy - ω(lx+ly)
v2 = vx + vy - ω(lx+ly)  
v3 = vx + vy + ω(lx+ly)
v4 = vx - vy + ω(lx+ly)
```

### 欧拉角
```
yaw   = atan2(2(qw·qz + qx·qy), 1 - 2(qy² + qz²))
pitch = asin(2(qw·qy - qz·qx))
roll  = atan2(2(qw·qx + qy·qz), 1 - 2(qx² + qy²))
```

---

## 考前检查清单

- [ ] PID三个参数的作用
- [ ] 坐标系变换公式（全局↔本体）
- [ ] 麦克纳姆轮速度计算
- [ ] ROS2节点创建方法
- [ ] 话题订阅和发布的代码
- [ ] 串口命令格式
- [ ] 电机类型和参数
- [ ] TF坐标系层级关系
- [ ] 四元数与欧拉角转换
- [ ] 常用ROS2命令

---

**提示**: 本手册配合《期末考试复习资料.md》使用效果更佳！

祝考试成功！🎓
