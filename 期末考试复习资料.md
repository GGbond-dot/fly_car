# 飞行小车项目期末考试复习资料

## 项目概述

这是一个基于 ROS2 的飞行小车控制系统项目，整合了激光雷达、PID控制、串口通信、蓝牙控制等多个模块。

### 系统架构

本项目采用分布式节点架构，主要包含以下核心模块：

1. **位置控制模块** (pid_control_pkg)
2. **串口通信模块** (uart_to_stm32)
3. **电机驱动模块** (car_driver)
4. **蓝牙通信模块** (bluetooth)
5. **激光雷达模块** (bluesea2)
6. **路径规划模块** (activity_control_pkg)
7. **地图构建模块** (my_carto_pkg)

---

## 一、PID控制算法

### 1.1 PID控制原理

PID（比例-积分-微分）控制器是工业控制中最常用的控制算法。

**基本公式：**
```
output = Kp * error + Ki * ∫error dt + Kd * d(error)/dt
```

其中：
- **Kp（比例系数）**：当前误差的放大系数，响应快但容易震荡
- **Ki（积分系数）**：消除稳态误差，但响应慢且可能引起超调
- **Kd（微分系数）**：预测误差变化趋势，减少超调和震荡

### 1.2 本项目中的PID实现

**文件位置**: `src/pid_control_pkg/src/pid_controller.cpp`

#### 关键特性：

1. **死区控制**（Deadzone）
   - 当误差小于死区值时，将误差设为0
   - 避免微小误差引起不必要的控制动作
   ```cpp
   if (std::fabs(current_error_) < deadzone_) {
       current_error_ = 0.0;
   }
   ```

2. **积分限幅**（Anti-windup）
   - 防止积分项累积过大
   - 限制积分值在 [-integral_limit_, integral_limit_] 范围内
   ```cpp
   if (integral_ > integral_limit_) {
       integral_ = integral_limit_;
   } else if (integral_ < -integral_limit_) {
       integral_ = -integral_limit_;
   }
   ```

3. **输出限幅**
   - 限制控制器输出在安全范围内
   ```cpp
   if (output > max_output_) {
       output = max_output_;
   } else if (output < min_output_) {
       output = min_output_;
   }
   ```

4. **微分滤波**
   - 使用低通滤波器平滑微分项，减少噪声影响
   ```cpp
   derivative_filtered = derivative_filter_alpha_ * prev_derivative_ +
       (1.0 - derivative_filter_alpha_) * derivative_raw;
   ```

### 1.3 位置控制器（PositionPIDController）

**控制目标**：
- X、Y 方向位置控制
- Z 方向（高度）控制
- Yaw 角度（偏航角）控制

**订阅话题**：
- `/target_position` (std_msgs::msg::Float32MultiArray): [x_cm, y_cm, z_cm, yaw_deg]
- `/height` (std_msgs::msg::Int16): 当前高度

**发布话题**：
- `/target_velocity` (std_msgs::msg::Float32MultiArray): [vx, vy, vz, vyaw]

**控制模式**：
```cpp
enum class ControlMode {
    NORMAL,    // 正常模式：全速控制
    SLOW,      // 慢速模式：降低速度限制
    LOCK_Y,    // 锁定Y轴：主要控制X方向
    LOCK_X,    // 锁定X轴：主要控制Y方向
    HOVER      // 悬停模式：精确位置保持
};
```

---

## 二、串口通信协议

### 2.1 UART to STM32 通信模块

**文件位置**: `src/uart_to_stm32/src/uart_to_stm32.cpp`

#### 通信参数：
- **串口设备**: `/dev/ttyUSB0`
- **波特率**: 921600
- **数据格式**: 8N1（8位数据，无校验，1位停止位）

#### 数据处理流程：

1. **订阅速度指令**
   - `/velocity_map` (geometry_msgs::msg::Twist): 全局坐标系速度
   - `/target_velocity` (std_msgs::msg::Float32MultiArray): 目标速度

2. **发布传感器数据**
   - `/height` (std_msgs::msg::Int16): 高度数据
   - `/is_st_ready` (std_msgs::msg::UInt8): STM32就绪状态
   - `/mission_step` (std_msgs::msg::UInt8): 任务步骤

### 2.2 电机驱动通信协议

**文件位置**: `src/car_driver/src/USART.py`

#### 命令格式：

所有命令以 `$` 开头，以 `#` 结尾。

**配置命令**：
```python
# 设置电机类型
"$mtype:{TYPE}#"

# 设置死区
"$deadzone:{DZ}#"

# 设置磁环线数
"$mline:{LINE}#"

# 设置减速比
"$mphase:{PHASE}#"

# 设置轮子直径
"$wdiameter:{WHEEL}#"
```

**控制命令**：
```python
# 速度控制（有编码器电机）
"$spd:{m1},{m2},{m3},{m4}#"

# PWM控制（无编码器电机）
"$pwm:{m1},{m2},{m3},{m4}#"

# 数据上传控制
"$upload:{mode1},{mode2},{mode3}#"
```

**数据接收格式**：
```python
# 总编码器数据
"$MAll:{value1},{value2},{value3},{value4}#"

# 实时编码器数据
"$MTEP:{value1},{value2},{value3},{value4}#"

# 电机速度数据 (mm/s)
"$MSPD:{speed1},{speed2},{speed3},{speed4}#"
```

---

## 三、坐标系变换与TF

### 3.1 TF（Transform）系统

ROS2使用TF库管理坐标系之间的变换关系。

**主要坐标系**：
- `map`: 全局地图坐标系（固定）
- `odom`: 里程计坐标系（相对固定，会有累积误差）
- `base_link`: 机器人本体坐标系（随机器人移动）
- `laser_link`: 激光雷达坐标系

### 3.2 坐标变换公式

#### 全局坐标系到本体坐标系：
```cpp
// 旋转变换矩阵
x_local = cos(θ) * x_global + sin(θ) * y_global
y_local = -sin(θ) * x_global + cos(θ) * y_global
```

#### 本体坐标系到全局坐标系：
```cpp
x_global = cos(θ) * x_local - sin(θ) * y_local
y_global = sin(θ) * x_local + cos(θ) * y_local
```

其中 `θ` 是机器人的偏航角（yaw angle）。

### 3.3 四元数与欧拉角转换

```cpp
// 四元数转欧拉角
tf2::Quaternion q;
tf2::fromMsg(transform.transform.rotation, q);
double roll, pitch, yaw;
tf2::Matrix3x3(q).getRPY(roll, pitch, yaw);

// 角度归一化到 [-π, π]
double normalizeAngle(double angle) {
    while (angle > M_PI) angle -= 2.0 * M_PI;
    while (angle < -M_PI) angle += 2.0 * M_PI;
    return angle;
}
```

---

## 四、麦克纳姆轮运动学

### 4.1 麦克纳姆轮特点

麦克纳姆轮是一种全向移动轮，可以实现：
- 前后移动
- 左右平移
- 原地旋转
- 任意方向斜向移动

### 4.2 逆运动学公式

给定机器人本体坐标系下的速度 (vx, vy, ω)，计算四个轮子的速度：

```cpp
// lx: 前后轮距的一半
// ly: 左右轮距的一半
double lx = wheel_base_ / 2.0;
double ly = track_width_ / 2.0;

// 四个轮子的速度计算
wheel_speed[0] = vx - vy - ω * (lx + ly);  // 前左轮
wheel_speed[1] = vx + vy - ω * (lx + ly);  // 前右轮
wheel_speed[2] = vx + vy + ω * (lx + ly);  // 后右轮
wheel_speed[3] = vx - vy + ω * (lx + ly);  // 后左轮
```

---

## 五、电机类型与参数配置

### 5.1 支持的电机类型

本项目支持5种电机类型（在 `USART.py` 中定义）：

1. **520电机** (MOTOR_TYPE = 1)
   - 减速比: 30
   - 磁环线: 11
   - 轮子直径: 67.00 mm
   - 死区: 1600

2. **310电机** (MOTOR_TYPE = 2)
   - 减速比: 20
   - 磁环线: 13
   - 轮子直径: 80.00 mm
   - 死区: 1300

3. **测速码盘TT电机** (MOTOR_TYPE = 3)
   - 减速比: 45
   - 磁环线: 13
   - 轮子直径: 68.00 mm
   - 死区: 1250

4. **TT直流减速电机** (MOTOR_TYPE = 4)
   - 减速比: 48
   - 死区: 1000
   - 注意：此类电机使用PWM控制而非速度控制

5. **L型520电机** (MOTOR_TYPE = 5)
   - 减速比: 40
   - 磁环线: 11
   - 轮子直径: 67.00 mm
   - 死区: 1600

### 5.2 电机参数说明

- **减速比（Phase）**: 电机输出轴转速与输入轴转速的比值
- **磁环线（Line）**: 编码器磁环的线数，用于测速
- **轮子直径（Diameter）**: 用于将电机转速转换为线速度
- **死区（Deadzone）**: 电机启动所需的最小PWM值

---

## 六、ROS2核心概念

### 6.1 节点（Node）

节点是ROS2中的基本执行单元，每个节点负责特定功能。

```cpp
// 创建节点
class MyNode : public rclcpp::Node {
public:
    MyNode() : Node("my_node_name") {
        // 初始化
    }
};

// 运行节点
int main(int argc, char** argv) {
    rclcpp::init(argc, argv);
    auto node = std::make_shared<MyNode>();
    rclcpp::spin(node);
    rclcpp::shutdown();
    return 0;
}
```

### 6.2 话题（Topic）通信

话题是ROS2中的异步通信机制。

**发布者（Publisher）**：
```cpp
// 创建发布者
publisher_ = create_publisher<std_msgs::msg::String>("topic_name", 10);

// 发布消息
auto message = std_msgs::msg::String();
message.data = "Hello";
publisher_->publish(message);
```

**订阅者（Subscriber）**：
```cpp
// 创建订阅者
subscription_ = create_subscription<std_msgs::msg::String>(
    "topic_name", 10,
    std::bind(&MyNode::callback, this, std::placeholders::_1));

// 回调函数
void callback(const std_msgs::msg::String::SharedPtr msg) {
    RCLCPP_INFO(get_logger(), "Received: %s", msg->data.c_str());
}
```

### 6.3 定时器（Timer）

定时器用于周期性执行任务。

```cpp
// 创建定时器（100ms执行一次）
timer_ = create_wall_timer(
    std::chrono::milliseconds(100),
    std::bind(&MyNode::timerCallback, this));

// 回调函数
void timerCallback() {
    // 周期性执行的代码
}
```

---

## 七、Launch文件

Launch文件用于同时启动多个节点并配置参数。

### 7.1 Python Launch文件示例

```python
from launch import LaunchDescription
from launch_ros.actions import Node

def generate_launch_description():
    return LaunchDescription([
        Node(
            package='package_name',
            executable='node_name',
            name='custom_node_name',
            parameters=[{
                'param1': value1,
                'param2': value2
            }],
            remappings=[
                ('old_topic', 'new_topic')
            ]
        )
    ])
```

---

## 八、关键知识点总结

### 8.1 控制系统设计要点

1. **PID参数调节**
   - 先调Kp，再调Kd，最后调Ki
   - Kp过大会震荡，过小响应慢
   - Ki用于消除稳态误差
   - Kd用于减少超调

2. **坐标系选择**
   - 控制算法通常在机器人本体坐标系中进行
   - 路径规划在全局坐标系中进行
   - 需要正确进行坐标变换

3. **死区处理**
   - 避免在目标点附近震荡
   - 节省能量消耗

### 8.2 通信协议设计要点

1. **数据帧格式**
   - 明确的起始符和结束符
   - 数据类型标识
   - 必要时添加校验和

2. **错误处理**
   - 超时检测
   - 数据格式验证
   - 重传机制

### 8.3 实时系统考虑

1. **更新频率**
   - 控制器频率: 50-100 Hz
   - 传感器读取: 根据传感器特性
   - 通信频率: 足够高以满足实时性要求

2. **时间同步**
   - 使用时间戳
   - 考虑通信延迟

---

## 九、常见问题与解决方案

### 9.1 串口通信失败

**问题**: 无法打开串口 `/dev/ttyUSB0`

**解决方案**:
```bash
# 检查串口设备是否存在
ls -l /dev/ttyUSB*

# 添加用户到dialout组
sudo usermod -a -G dialout $USER

# 修改串口权限
sudo chmod 666 /dev/ttyUSB0
```

### 9.2 PID控制震荡

**问题**: 机器人在目标点附近震荡

**解决方案**:
1. 减小Kp值
2. 增大死区范围
3. 增加Kd值以增加阻尼
4. 检查传感器噪声

### 9.3 TF查询失败

**问题**: `Could not transform odom to base_link`

**解决方案**:
1. 检查相关节点是否正常发布TF
2. 使用 `ros2 run tf2_tools view_frames` 查看TF树
3. 确保坐标系名称正确

---

## 十、项目编译与运行

### 10.1 编译项目

```bash
# 进入工作空间
cd /path/to/fly_car

# 编译所有包
colcon build

# 编译特定包
colcon build --packages-select pid_control_pkg

# 清理并重新编译
rm -rf build install log
colcon build
```

### 10.2 运行节点

```bash
# 设置环境变量
source install/setup.bash

# 运行单个节点
ros2 run package_name executable_name

# 运行launch文件
ros2 launch package_name launch_file.py

# 查看话题
ros2 topic list
ros2 topic echo /topic_name

# 查看节点
ros2 node list
ros2 node info /node_name
```

---

## 十一、考试重点

### 必须掌握的概念：

1. **PID控制算法**
   - 三个参数的作用
   - 死区、限幅的目的
   - 积分饱和问题

2. **坐标系变换**
   - 旋转矩阵
   - 四元数与欧拉角的关系
   - 全局坐标系与本体坐标系的转换

3. **麦克纳姆轮运动学**
   - 四个轮子速度的计算公式
   - 为什么能实现全向移动

4. **ROS2基础**
   - 节点、话题、订阅、发布的概念
   - 如何创建节点
   - 如何订阅和发布话题

5. **串口通信**
   - 波特率的概念
   - 数据帧格式
   - 起始符和结束符的作用

### 重要公式：

1. PID控制输出：
   ```
   u(t) = Kp·e(t) + Ki·∫e(t)dt + Kd·de(t)/dt
   ```

2. 坐标旋转（全局→本体）：
   ```
   x_local = cos(θ)·x_global + sin(θ)·y_global
   y_local = -sin(θ)·x_global + cos(θ)·y_global
   ```

3. 麦克纳姆轮速度：
   ```
   v_FL = vx - vy - ω(lx + ly)
   v_FR = vx + vy - ω(lx + ly)
   v_RR = vx + vy + ω(lx + ly)
   v_RL = vx - vy + ω(lx + ly)
   ```

---

## 总结

本项目综合运用了：
- **控制理论**: PID控制算法
- **机器人学**: 运动学、坐标变换、TF系统
- **嵌入式通信**: 串口协议、数据解析
- **软件工程**: ROS2框架、模块化设计

理解这些核心概念和它们之间的关系是掌握本项目的关键。

祝考试顺利！🚀
